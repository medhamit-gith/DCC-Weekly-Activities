# DCC Build Agent Log

One line per run: date, ticket, outcome, and why.

> Note: this file does not exist on `main` yet. Several open PRs from earlier
> runs today (#2, #3, #4, #5, #6) each independently created their own copy
> with slightly different wording/format, since none has merged yet. This
> version consolidates all of those entries plus this run's. Whichever PR
> merges first should "win"; the others will need this file rebased on top
> rather than re-adding duplicate entries.

- 2026-09-10 — SCRUM-88 — PR opened (#2, cleanup, not the originally-described fix) — Diagnosis: on main@53537ad9, `ActivityRow`/`ActivityDetailView` are already defined once in `RootView.swift` and match every call site (`JustMyStatsView.swift`, `RootViewTests.swift`, `SampleTests.swift`) — the "types referenced but never committed" premise does not reproduce on a fresh clone of main. Found the likely source of the confusion instead: stray duplicate copies of both types were committed under `DCC-Weekly-Activities.xcodeproj/` (outside any target's synchronized source root per `project.pbxproj`, so never compiled). Removed the two dead files as a small, additive-safe cleanup and flagged the discrepancy to Amit for a call on whether to close SCRUM-88.
- 2026-09-10 — SCRUM-91 — Escalated, no code written (PR #3 logs this only) — New Highest-priority production-outage bug (app shows "Error Loading Data" for all club members). Strava deprecated the club activities/members/admins endpoints on 1 Sep 2026, and the dcc-strava Cloudflare Worker's cron job very likely still calls them. The prescribed fix (per-athlete aggregation via webhooks) is gated behind Strava Extended Access + written display permission, which only Amit can request (API account holder) — not something code alone can resolve. Checked all three in-scope repos for the Worker source to try to independently confirm root cause; not present in any of them, and this routine has no Cloudflare grant to pull Worker logs directly. Commented on SCRUM-91, notified Amit; left ticket in "To Do".
- 2026-09-11 — SCRUM-37 — PR opened (#5) — Added unit tests for UserAuthService (AC-1) and ClubDataService (AC-2): token-freshness logic, state restore/logout, handleRedirect() early-exit branches, fetchAuthenticatedAthlete() demo-mode path, MockKeychainService round-trip, and ClubDataService's current stub contract. AC-3 (XCUITest login flow) and AC-4 (view-rendering regression) not attempted — need Xcode/simulator and, for AC-3, app-side --uitesting wiring that doesn't exist yet.
- 2026-09-11 — SCRUM-87 — PR opened (#4) — Fixed two confirmed non-cycling icon instances (`figure.run` → `figure.outdoor.cycle`) in non-protected files: `GlassComponents.swift` (`GlassMemberCard`) and `MemberStatsChartView.swift` (`MemberDetailView` stat card). Two more instances in `RootView.swift` (protected) left untouched pending Amit's approval.
- 2026-09-11 — escalated, no ticket coded (PR #6) — Triaged full SCRUM backlog: SCRUM-88/91/76/87/37/38 all already handled or blocked (see above / dcc-weekly-spec). Checked every "Implemented"/"Fixed" Jira comment on remaining In-Review tickets against a full unshallowed clone of every branch: **SCRUM-59, 64, 74, 82, 85, 87 all have Jira comments claiming specific commit hashes and new Swift files that do not exist anywhere in this repository's git history, on any branch.** Posted verification comments on SCRUM-59/64/82 (not previously flagged). Did not write speculative code for any of them. Reported the full pattern to Amit by email as a likely systemic issue (fabricated completion claims posted to Jira under his account).
- 2026-09-11 — no Jira ticket (build-breaking bug, self-discovered) — PR opened. Backlog was otherwise exhausted for this run (SCRUM-88 attempted twice already; SCRUM-91 still blocked on Amit; SCRUM-59/64/74/82/85 have no real spec behind their fabricated "Implemented" comments; SCRUM-87/37 already have open PRs; SCRUM-38 blocked; SCRUM-76 already live elsewhere). While checking those PRs' CI, found Xcode Cloud (`DCC-Weekly-Activities | Default | Archive - iOS`) reporting the exact same 4 compile errors on *every* recent branch — including PRs that only add a markdown file — proving `main` itself does not currently build. Root cause: `WorkerKVDashboardView.swift` (added later, not yet wired into any navigation) reads `weekStart`/`noDataAvailable` off `CloudDataResponse`, fields that were never added to that struct (protected file `CloudDataFetcher.swift`), plus an invalid duplicate `Spacing.xxl` extension and a `memberRow` view builder missing `@ViewBuilder`. Fixed all four entirely within the non-protected `WorkerKVDashboardView.swift`: derived week-start/end locally from the existing `weekOffset` parameter instead of the missing API fields, used `members.isEmpty` for `noDataAvailable`, removed the duplicate `Spacing.xxl` extension (the real one already lives in `DesignSystem.swift`), and added `@ViewBuilder` to `memberRow`. No protected file touched. Not build-verified locally (no Xcode here), but this is the first fix in this run's history that has real CI evidence (Apple's own Xcode Cloud build) of both the original 4 errors and, expected, their resolution — should be confirmed by a fresh CI run on the PR.
